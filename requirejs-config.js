var config = {
    paths: {
        'nav-tag': "js/nav-tag",
        'accordion': "js/accordion"
    },
    shim: {
        'nav-tag': {
            deps: ['jquery']
        },

        'accordion': {
            deps: ['jquery']
        }
    }
};